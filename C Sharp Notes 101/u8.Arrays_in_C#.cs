/*using System.Runtime.InteropServices;

namespace C_Sharp_Notes_101;


// An array is a collection of elements of a single data type stored in adjacent memory locations.

// An array is a collection of related values placed in contigous memory locations and these values are referenced 
// using a common array name.

// Simplifies the task of maintaining these values.

// Each value is referred to as an element.
// These elements are accessed using subscripts or index numbers that determine the position of the element in the array list.





public class Arrays_in_C_
{
 static void Main(string[] args)
    {
        int[] numbers = new int[5];
        numbers[0] = 1;
        numbers[1] = 2; 
        numbers[2] = 3; 
        numbers[3] = 4;
        numbers[4] = 5;
        Console.WriteLine(numbers[0]);
        Console.WriteLine(numbers.Length);

        *//* string[] name = new string[] { "python", "javascript", "C#" };
         Console.WriteLine(name[2]);
         name[0] = "python django";
         Console.WriteLine(name[0]);*/


      /*  string[] address = { "birtamode", "paris", "london" };
        Console.WriteLine(address[0]);*//*
    }
}*/